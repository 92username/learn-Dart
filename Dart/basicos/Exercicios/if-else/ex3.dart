// 3. Verifique se uma variável booleana chamada isRaining está verdadeira
// ou falsa e exiba uma mensagem para levar ou não o guarda-chuva.

void main() {
  // Verificar se está chovendo
  bool isRaining = true;

  // Verificar se deve levar o guarda-chuva
  if (isRaining) {
    print('Está chovendo, leve o guarda-chuva!');
    // ignore: dead_code
  } else {
    print('Não está chovendo, não precisa levar o guarda-chuva.');
  }
}
